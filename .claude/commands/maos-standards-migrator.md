# maos-standards-migrator

Run the `00_ondemand/standards-migrator` pipeline agent. Ad-hoc agent that scans the consuming repo for existing knowledge files and ground-trut….

## Input

`$ARGUMENTS` — GitHub issue or PR number (e.g. `42`)

## Instructions

Follow the `run-agent` command with pre-filled arguments:
`run-agent 00_ondemand/standards-migrator $ARGUMENTS`
