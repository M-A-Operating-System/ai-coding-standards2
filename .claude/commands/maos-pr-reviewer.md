# maos-pr-reviewer

Run the `03_execute/pr-reviewer` pipeline agent. Reviews the draft PR for this issue after CI passes.

## Input

`$ARGUMENTS` — GitHub issue or PR number (e.g. `42`)

## Instructions

Follow the `run-agent` command with pre-filled arguments:
`run-agent 03_execute/pr-reviewer $ARGUMENTS`
