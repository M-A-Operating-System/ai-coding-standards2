<!-- ai-agile/artefact/v1 by 01_product_docs/prd-docs-updater -->
## Product docs check -- no updates required

Reviewed `docs/product/` against the approved PRD. The target state described in the PRD -- that `--print-prompt`'s `allowed_tools` matches the real spawn's `--allowedTools` -- is already correctly specified in the existing scenario in `docs/features/orchestrator.md` ("/run-agent obtains its tool allowlist from the orchestrator instead of hand-parsing frontmatter"). This is a bug fix; the code was wrong, not the docs.

### Feature file (docs/features/)

- `docs/features/orchestrator.md` existed; four scenarios appended:
  - "Resolve-only returns the same allowlist as a real spawn" (appended)
  - "The defaults are included" (appended)
  - "A /run-agent session can remove its own scope file" (appended)
  - "The drift is caught by a test, not by inspection" (appended)
