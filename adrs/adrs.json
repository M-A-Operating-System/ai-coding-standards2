{
  "$schema": "../pipeline/schemas/standards.schema.json",
  "version": "1.0.0",
  "scope": "project",
  "description": "Project ADRs for ai-coding-standards2 itself -- this repo dogfoods its own orchestrator, so it records its own architectural decisions here the same way any consuming repo would.",
  "adrs": [
    {
      "id": "ADR-001",
      "title": "The orchestrator coordinates only -- it decides completion, sequencing, and controls; it does not implement process logic",
      "status": "accepted",
      "rationale": "STD-ARCH-035 already states this ('pipeline/pipeline_orchestrator.py decides which step runs next and records the outcome. It must not contain business-process logic.'); this ADR exists to make the boundary concrete and actionable rather than aspirational, and to give future coder/pr-reviewer runs a specific line to hold new code against.",
      "context": "A survey of pipeline_orchestrator.py (issue #445 review cycle, 2026-09-21) found roughly 900-1000 of its ~8600 lines (~11%) are process/business logic rather than coordination: git worktree creation/removal and the module-level globals a signal handler uses to undo them mid-run (STD-ARCH-035's own named anti-pattern), the push/salvage/stray-file-detection sequence, a `gh` CLI apt-get bootstrap, GitHub Actions metrics-record construction, a hand-rolled todos-block markdown patcher, and announcement/artefact comment formatting. Some of this predates the standard and has already been partially addressed (e.g. `_append_metrics_record` and `_run_lifecycle_scripts` already delegate to `.github/scripts/*.sh`). The immediate trigger: while addressing a pr-reviewer finding on PR #493 (issue #445), the fix under consideration was to add a code comment to pipeline_orchestrator.py justifying why more git logic was being added inline, rather than questioning whether it should be inline at all.",
      "decision": "pipeline_orchestrator.py's job is limited to three things: (1) deciding what has completed -- reading labels and artefact records, comparing a step's recorded subject against the current one to detect staleness; (2) deciding what runs next -- evaluating triggers, dependencies, exclusions, and eligibility; (3) owning the controls -- the `:wip` mutex, retries, human gates, and review-loop cycle counting. Everything else is process logic and belongs in a script under `.github/scripts/` or an agent prompt under `.claude/agents/`, per STD-ARCH-035: filesystem setup/teardown, git operations (worktree, commit, push, recovery), GitHub API workflows beyond reading label/comment state (branch bootstrap, metrics-record construction), and generating step-specific artefact content (comment bodies, todos-block patches). A change that adds new process logic to pipeline_orchestrator.py is not justified by matching existing inline precedent in the same file -- that precedent is itself the debt this ADR names, not a pattern to extend.",
      "consequences": "New pipeline capabilities are built as scripts or agents first; a change that cannot avoid touching pipeline_orchestrator.py should extend dispatch/state logic only, not add a new process-logic function. Existing inline process logic (the worktree/git family, the two in-flight-state globals and their signal handler, the metrics/comment/todos-patch formatting helpers) is tracked as pre-existing debt against this decision, not additional precedent for future inline logic. pr-reviewer should treat new process logic landing in pipeline_orchestrator.py as a standards finding (STD-ARCH-035) on its own terms, independent of whether it resembles code already in the file.",
      "approved_by": "agbush2",
      "approved_at": "2026-09-21"
    },
    {
      "id": "ADR-002",
      "title": "STD-ARCH-035 exceptions confirmed by issue #495's extraction pass: four functions that stay inline",
      "status": "accepted",
      "authorises_exception_to": ["STD-ARCH-035"],
      "rationale": "ADR-001 names inline process logic as pre-existing debt to be worked off, not precedent to extend, but does not itself authorise any exception -- pr-reviewer correctly flagged new inline git logic against it on PR #493 (finding SC-001) precisely because it granted no waiver. Issue #495 then worked through ADR-001's named debt function by function: _ensure_gh_cli, todos-block patching, _recover_unpushed_commits, and the _CURRENT_WIP/_CURRENT_WORKTREE globals with their signal handler were all successfully extracted or removed. Four functions were investigated and found to have a genuine, reasoned justification for staying inline rather than a lack of effort -- this ADR records that justification so a future coder or pr-reviewer run does not re-litigate the same investigation from zero, and so 'stays inline' is visibly a decision, not silent debt.",
      "context": "Two distinct reasons surfaced during the extraction pass, corresponding to two pairs of functions:\n\n1. Load-bearing, no-fallback operations (_push_step_branch, _create_run_worktree/_remove_run_worktree). Both explicitly raise or return a hard failure rather than degrade -- a step cannot commit without a pushed branch, and cannot run at all without its worktree. The script-resolution mechanism this issue's other extractions use (_orchestration_script_path, with its origin/main fallback and skip-if-still-missing behaviour) is safe only for genuinely best-effort operations: ensure-gh-cli.sh and recover-unpushed-commits.sh degrade gracefully if unresolvable because nothing else depends on them succeeding. Routing a load-bearing operation through the same resolver would reintroduce issue #196's exact regression class, which a standing regression test (test_pushing_a_step_s_commits_needs_no_script_on_the_branch) exists specifically to keep fixed: 'a branch carrying no orchestration scripts at all still pushes... the resolver this class covers remains for the scripts that are still scripts.'\n\n2. Infrastructure tightly coupled to the orchestrator's own types and HTTP client (_ensure_metrics_branch; _build_agent_metrics/_build_scripted_metrics/_build_step_metrics; _build_opening_announcement/_build_closing_announcement). _ensure_metrics_branch calls GitHubClient._get/_post/_put, which carry real retry-with-backoff behaviour (429/5xx retries, Retry-After honouring) that a bash `gh api` rewrite would lose or have to non-trivially reimplement, for a code path that runs at most once per repository (branch creation, guarded by an early-exists check). The metrics-builder and announcement functions all take AgentDef/WorkItem/AgentRunResult directly and call other orchestrator-internal functions (e.g. step_branch) -- unlike todos_patch.py's successful extraction (pure string in, string out, zero orchestrator-type coupling), these are not pure functions with a clean boundary; decoupling them would mean refactoring their call sites to extract primitive fields first, not just moving code.",
      "decision": "These four functions (and the metrics/announcement family they represent) remain inline in pipeline_orchestrator.py. This is a standing, reasoned exception to STD-ARCH-035, not a queued extraction: a future change should not attempt to force them into the same script-resolution or pure-module patterns this issue used for the rest of the file's process logic, without first addressing the specific reason each stays (load-bearing no-fallback semantics, or orchestrator-type/HTTP-client coupling) with its own design.",
      "consequences": "pr-reviewer should not flag these four functions (or new code that follows the same shape -- a load-bearing git operation, or logic genuinely coupled to GitHubClient's retry machinery or the AgentDef/WorkItem type system) as an unaddressed STD-ARCH-035 finding on their own; this ADR is the citable waiver. A change that adds NEW inline process logic not matching one of these two reasoned categories is not covered and should still be flagged, per ADR-001's own consequences.",
      "approved_by": "agbush2",
      "approved_at": "2026-09-21"
    },
    {
      "id": "ADR-003",
      "title": "STD-SEC-022 exception: scripts doing a plain git push inherit the orchestrator's environment wholesale, not a named GIT_CONFIG_* allowlist",
      "status": "accepted",
      "authorises_exception_to": ["STD-SEC-022"],
      "rationale": "STD-SEC-022 requires a named allowlist, but explicitly allows 'broad inheritance of the ambient environment by a first-party trusted script' as 'a deliberate, commented decision citing an ADR' (its own acceptance criteria). This is that ADR, for exactly one narrow pattern: a script whose only need is the git-push credential main() sets up as GIT_CONFIG_COUNT/GIT_CONFIG_KEY_N/GIT_CONFIG_VALUE_N.",
      "context": "pr-reviewer flagged recover-unpushed-commits.sh's invocation (no env= override) as an unaddressed STD-SEC-022 finding on PR #497 (issue #495, cycle 4, finding SA-001). The fix attempted was a static named allowlist: _RECOVER_UNPUSHED_COMMITS_ENV_VARS = (\"PATH\", \"HOME\", \"GIT_CONFIG_COUNT\", \"GIT_CONFIG_KEY_0\", \"GIT_CONFIG_VALUE_0\"). This broke TestRecoveringABranchLeftAhead's own tests: GIT_CONFIG_COUNT/KEY_N/VALUE_N is a dynamically-numbered family, not a fixed set of three names. main() overwrites it to exactly one entry (COUNT=1, KEY_0/VALUE_0 the auth header) when it runs for real, but this repo's own sandboxed dev environment independently sets three entries of its own (COUNT=3, KEY_0..KEY_2) for git's github.com routing, unrelated to the orchestrator. A static allowlist naming only *_0 picked up the sandbox's COUNT=3 but forwarded just one of its three entries, producing a broken, inconsistent git config in the subprocess -- confirmed by reproducing the exact test failure this caused. The same fragility applies to any environment that sets its own baseline GIT_CONFIG_* entries for unrelated reasons; the risk is not specific to this one sandbox.",
      "decision": "A script whose only environment need is this git-push credential (_salvage_exhausted_worktree's call to salvage-exhausted-worktree.sh, established in PR #493; _recover_unpushed_commits' call to recover-unpushed-commits.sh, this issue) is invoked with no env= override, inheriting the calling process's full environment, rather than attempting to enumerate GIT_CONFIG_COUNT's dynamically-numbered siblings by static name. This matches the pattern _git_in's own inline `git push` calls already use, unchanged by this issue.",
      "consequences": "pr-reviewer should not flag a script matching this exact shape (a plain `git push`/`git fetch`, no `gh` CLI use, no auth header it builds itself) for a missing env= allowlist on its own; this ADR is the citable waiver, alongside each script's own MI-7 identity-exemption entry in test_sec022_env_allowlists.py's _IDENTITY_EXEMPT. A script that does anything beyond plain git operations over this one credential is not covered and should still build a named allowlist.",
      "approved_by": "agbush2",
      "approved_at": "2026-09-21"
    }
  ]
}
