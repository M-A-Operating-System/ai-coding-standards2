{
  "$schema": "../pipeline/schemas/standards.schema.json",
  "version": "1.0",
  "scope": "org",
  "category": "security",
  "description": "Security standards enforced on every diff. Most rules are adr_overridable: false — there is no legitimate business justification for committing secrets, enabling injection, or skipping input validation at trust boundaries.",
  "standards": [

    {
      "id": "STD-SEC-001",
      "title": "No plaintext secrets in source",
      "description": "Credentials, API keys, tokens, private keys, and passwords must never appear in source files, test fixtures, or commit messages. All secrets must be injected via environment variables, a secrets manager, or a CI/CD vault at runtime.",
      "rationale": "Plaintext secrets committed to source are exposed to every person with repo access, every CI runner log, and every fork. They are the leading cause of supply-chain compromise and are effectively permanent once in git history.",
      "acceptance_criteria": [
        "No string literal in the diff matches the pattern of a known secret format (AWS key, GitHub PAT, private key PEM block, bearer token, connection string with embedded password).",
        "No test fixture file in the diff contains a real credential — synthetic values like 'test-secret-placeholder' are acceptable; real-looking tokens are not.",
        "Configuration that requires a secret uses an environment variable reference (e.g. os.environ['API_KEY']) or a named secrets-manager path — never a hardcoded value.",
        "Any file added in the diff whose name suggests secret content (.env, *.pem, *.p12, *_key.json, credentials.*) is absent from tracked source and present in .gitignore."
      ],
      "anti_patterns": [
        "API_KEY = 'sk-live-abc123...' in any source file.",
        "A test that creates a real token and stores it in a fixture file.",
        "A connection string like 'postgresql://user:password@host/db' hardcoded in config.",
        "A private key PEM block embedded in a YAML or JSON config file.",
        "Logging a full Authorization header value at DEBUG level."
      ],
      "adr_overridable": false,
      "applies_to": ["coder", "pr-reviewer"]
    },

    {
      "id": "STD-SEC-002",
      "title": "Validate and sanitise only at system boundaries",
      "description": "All data crossing a trust boundary — user input, external API responses, file uploads, webhook payloads, inter-service messages — must be validated and sanitised before use. Internal function calls between trusted modules do not require defensive re-validation.",
      "rationale": "Over-validating internal calls adds noise and false safety signals while under-validating boundary inputs is the root cause of injection attacks, data corruption, and privilege escalation.",
      "acceptance_criteria": [
        "Every HTTP request handler, CLI argument parser, and file reader in the diff validates the shape and type of its input before passing it to internal logic.",
        "Validation uses an explicit allowlist (permitted characters, permitted values, schema) rather than a blocklist of known-bad patterns.",
        "Validation errors produce a structured, user-facing error — they do not propagate raw untrusted data to downstream functions.",
        "Internal helpers that are only called by trusted callers in the same module do not duplicate the boundary validation already performed by their caller."
      ],
      "anti_patterns": [
        "Accepting a filename from user input and passing it directly to open() without path traversal checks.",
        "Deserialising a JSON payload from a webhook and assuming all fields are present and correctly typed.",
        "Re-validating function arguments inside a private helper that is only called after the public API has already validated them.",
        "Using a blocklist of 'bad characters' instead of an allowlist of permitted characters for user-supplied strings."
      ],
      "adr_overridable": false,
      "applies_to": ["coder", "pr-reviewer"]
    },

    {
      "id": "STD-SEC-003",
      "title": "No shell injection via unsanitised input",
      "description": "Shell commands constructed from external input must use argument list form (subprocess with a list, not a string) or parameterised equivalents. String interpolation of untrusted data into shell commands is forbidden.",
      "rationale": "Shell injection is in the OWASP Top 10 and trivially exploitable. A single unvalidated value in a shell string can result in arbitrary code execution, data exfiltration, or privilege escalation.",
      "acceptance_criteria": [
        "Every subprocess call in the diff that originates from user-controlled data uses a list of arguments (subprocess.run(['cmd', arg], ...)) rather than a shell string (subprocess.run(f'cmd {arg}', shell=True)).",
        "No os.system(), os.popen(), or eval() call in the diff incorporates data derived from user input, environment variables set by callers, or external API responses without explicit quoting via shlex.quote().",
        "Template strings used to build shell commands contain only developer-controlled literals — never f-strings or .format() calls with external data.",
        "If shell=True is required, the rationale is documented inline and the arguments are sanitised with shlex.quote()."
      ],
      "anti_patterns": [
        "subprocess.run(f'git clone {user_url}', shell=True) where user_url comes from a form field.",
        "os.system('rm -rf ' + path) where path is derived from a request parameter.",
        "Constructing a shell pipeline as a single string and passing it to Popen.",
        "Using shell=True without any quoting for convenience rather than necessity."
      ],
      "adr_overridable": false,
      "applies_to": ["coder", "pr-reviewer"]
    },

    {
      "id": "STD-SEC-004",
      "title": "No eval or exec on untrusted data",
      "description": "Python eval(), exec(), compile(), and equivalent dynamic execution functions must not receive data that originates from user input, external APIs, file contents, or any other untrusted source.",
      "rationale": "Executing arbitrary code from untrusted input is an immediate remote code execution (RCE) vulnerability with no safe mitigation short of total elimination.",
      "acceptance_criteria": [
        "No eval() or exec() call in the diff takes a string constructed from or containing user-supplied data.",
        "If eval() is used on a developer-controlled literal (e.g. evaluating a constant expression at module load), the literal is either a module-level constant or loaded from a vetted internal file — never from a request, config file, or environment variable that could be user-controlled.",
        "Dynamic import mechanisms (importlib, __import__) in the diff resolve only to module names from a closed allowlist — they do not interpolate user input into the module path."
      ],
      "anti_patterns": [
        "eval(request.json()['formula']) to evaluate a user-supplied expression.",
        "exec(open(user_supplied_path).read()) to run a script from a user-chosen file.",
        "Using eval() to parse a config value when json.loads() or ast.literal_eval() would suffice.",
        "Dynamic __import__(user_plugin_name) without validating user_plugin_name against a known allowlist."
      ],
      "adr_overridable": false,
      "applies_to": ["coder", "pr-reviewer"]
    },

    {
      "id": "STD-SEC-005",
      "title": "Pin all production dependencies",
      "description": "Every package listed in production dependency files (requirements.txt, pyproject.toml dependencies, package.json dependencies, Gemfile) must be pinned to an exact version or a version range no wider than a patch band. Development-only and test dependencies may use wider ranges.",
      "rationale": "Unpinned dependencies allow supply-chain attacks: a malicious release of a transitive dependency can be silently picked up by the next CI run, injecting arbitrary code into production builds.",
      "acceptance_criteria": [
        "Every package in the production dependency list introduced or modified in the diff specifies an exact version (==X.Y.Z in pip, X.Y.Z in npm) or a patch-level upper bound (>=X.Y.Z,<X.Y+1).",
        "No production dependency uses an open upper bound (>=X) or a wildcard (*).",
        "New dependencies added in the diff appear in the project's lock file (requirements.lock, package-lock.json, Pipfile.lock, poetry.lock) with resolved hashes where the toolchain supports it.",
        "Dev/test dependency files (requirements-dev.txt, devDependencies) are exempt from exact pinning but must still specify a minimum version."
      ],
      "anti_patterns": [
        "requests>=2.0 in requirements.txt with no upper bound.",
        "Adding a new npm package to dependencies without updating package-lock.json.",
        "Using a * or latest version specifier for any production dependency.",
        "Omitting a transitive dependency from the lock file after adding a new direct dependency."
      ],
      "adr_overridable": true,
      "applies_to": ["coder", "pr-reviewer"]
    }

  ]
}
