{
  "$schema": "../pipeline/schemas/standards.schema.json",
  "version": "1.0",
  "scope": "org",
  "category": "architecture",
  "description": "Org-level architecture standards: reuse, scope discipline, error handling, data-access boundaries, and API/performance rules that apply to every project installing AI Agile.",
  "standards": [
    {
      "id": "STD-ARCH-001",
      "title": "Reuse before writing new code",
      "description": "Before implementing new logic, search the codebase for existing functions, classes, or modules that already solve the same problem or a close approximation of it. Extend existing code rather than duplicating it.",
      "rationale": "Duplicate logic diverges over time, creates inconsistent behaviour, and multiplies the maintenance surface. The codebase already contains tested, deployed solutions \u2014 use them.",
      "acceptance_criteria": [
        "No function or method in the diff duplicates logic that is already present in another module at the time of writing.",
        "If an existing utility solves 80%+ of the need, the diff extends or calls that utility rather than reimplementing the behaviour from scratch.",
        "Any deliberate duplication includes an inline comment explaining why the existing implementation could not be reused."
      ],
      "anti_patterns": [
        "Copying a block from one module and pasting it with minor edits into another.",
        "Reimplementing a sort, filter, or transformation already present in a sibling module.",
        "Creating a new helper that is functionally equivalent to an existing one with a different name."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-002",
      "title": "Abstract only at two concrete call sites",
      "description": "Do not create helpers, base classes, mixins, decorators, or utility modules unless they have at least two distinct, concrete call sites in the codebase at the time of writing. Three similar lines of code are not sufficient justification for a new abstraction.",
      "rationale": "Premature abstractions introduce indirection that obscures intent, makes code harder to read, and must be maintained even when requirements change. A duplication that is removed too early often results in an abstraction that fits neither call site well.",
      "acceptance_criteria": [
        "Every new function, class, or module introduced in the diff has at least two concrete, distinct call sites visible in the repo at the time the PR is written.",
        "New base classes and mixins have at least two concrete subclasses in the diff or the existing codebase.",
        "New decorator or wrapper functions are applied to at least two target functions in the diff or existing codebase."
      ],
      "anti_patterns": [
        "A helper function called only once.",
        "A base class with exactly one subclass.",
        "A utility module with a single export used by a single caller.",
        "Extracting a two-line block into a named function 'for readability' when the block is only used once."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-003",
      "title": "No speculative generality",
      "description": "Do not add parameters, configuration options, plugin hooks, strategy objects, or extension points for anticipated future requirements that have no current specification in the product docs or active issues. Solve the problem as stated, not a generalised version of it.",
      "rationale": "Speculative features are never free \u2014 they add complexity, surface area for bugs, and cognitive load for future readers, even if they are never actually used. YAGNI (You Aren't Gonna Need It) applies to every layer of the stack.",
      "acceptance_criteria": [
        "Every parameter, config key, and extension point introduced in the diff is exercised by at least one real call site in the same diff or existing codebase.",
        "Optional parameters whose only caller always passes the default value are removed or never introduced.",
        "No code path exists in the diff that is not reachable from any call site present at the time of writing.",
        "Abstract base classes, interfaces, and protocols are only introduced when multiple concrete implementations exist or are required by the current issue's acceptance criteria."
      ],
      "anti_patterns": [
        "Adding a `strategy=` or `backend=` parameter with only one value ever passed.",
        "A plugin registry or hook system with zero registered plugins in the diff.",
        "A config flag that is always `True` or always `False` at every call site.",
        "An abstract method defined 'for future subclasses' with no current implementation."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-004",
      "title": "Implementations scope to stated acceptance criteria",
      "description": "Every line of new behaviour in a diff must be traceable to an acceptance criterion in the approved PRD, an existing standard in this file, or an approved ADR. Behaviour that is not required by any of these sources must not be introduced.",
      "rationale": "Out-of-scope additions bypass product approval gates, make PRs harder to review, and risk introducing untested behaviour into production. Scope creep in code is the dual of scope creep in planning.",
      "acceptance_criteria": [
        "For each new behaviour visible in the diff, the coder can cite the PRD acceptance criterion, STD ID, or ADR ID that requires it.",
        "No new user-visible behaviour (API surface, config keys, labels, log output) exists in the diff without a corresponding PRD acceptance criterion.",
        "New constants, flags, and configuration fields introduced for a current feature are marked clearly as belonging to that feature, not designed as general infrastructure."
      ],
      "anti_patterns": [
        "Adding concurrency controls when the issue only asks for retry logic.",
        "Introducing a metrics endpoint when the issue asks for a config change.",
        "Adding a CLI flag 'while we're here' that is not in the PRD."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-005",
      "title": "Keep functions and modules focused",
      "description": "Functions should do one thing. Modules should own one concern. When a function grows beyond approximately 40 lines or handles more than one distinct responsibility, split it. When a module imports from more than two unrelated domains, consider splitting it.",
      "rationale": "Small, focused units are easier to test, easier to reuse, and easier to reason about. Long functions with multiple responsibilities are the primary source of hidden coupling.",
      "acceptance_criteria": [
        "No new function in the diff exceeds 50 lines (excluding docstring and blank lines) unless the lines are a single flat sequence with no branching \u2014 e.g. a long match/switch statement.",
        "Functions introduced in the diff have a single clearly-stated purpose that can be described in one sentence without using 'and'.",
        "No module introduced in the diff imports from more than three unrelated domains at the same abstraction level."
      ],
      "anti_patterns": [
        "A function named `process_and_validate_and_save()`.",
        "A 200-line function containing distinct phases separated by blank lines and comments.",
        "A module that is both a data model and a network client."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-006",
      "title": "Fix now, not later \u2014 no deferred small defects",
      "description": "If a defect, inconsistency, dead code path, stale comment, incorrect type annotation, or other small correctness issue is visible while working on a diff, fix it in the same PR rather than deferring it. Do not introduce or perpetuate TODO/FIXME markers for issues that can be resolved immediately.",
      "rationale": "Small deferred items compound. A codebase full of acknowledged-but-unresolved defects degrades reviewer confidence, slows onboarding, and creates a ratchet where each PR adds more debt than it removes. The cheapest time to fix a defect is when you are already reading the affected code.",
      "acceptance_criteria": [
        "No TODO or FIXME comment is introduced in the diff unless it references an open issue number (e.g. '# TODO(#123)') and the linked issue is created before the PR is merged.",
        "Existing TODO/FIXME comments in files touched by the diff are either resolved inline or converted to tracked issues \u2014 they are not left as-is.",
        "Dead code (unreachable branches, unused imports, unused variables) visible in files touched by the diff is removed rather than left in place.",
        "Stale inline comments that describe behaviour that no longer exists are removed or corrected.",
        "Type annotations that are visibly wrong or missing on functions touched by the diff are corrected.",
        "A fix is only deferred when the coder documents in the PR description exactly why it cannot be resolved in this PR and links a follow-up issue."
      ],
      "anti_patterns": [
        "Adding '# TODO: clean this up later' without an issue number.",
        "Leaving an unreachable `else` branch in a function you are modifying.",
        "Removing a feature but leaving its import and dead helper function.",
        "Correcting a bug in one call site but leaving identical buggy code in the sibling call site in the same file.",
        "Commenting out code instead of deleting it.",
        "Leaving a `pass` or `raise NotImplementedError` in a method that is called in the current execution path."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-007",
      "title": "Do not open issues for work that can be done in the current PR",
      "description": "Agents must not file new GitHub issues as a substitute for doing work that is within their current scope. If a defect, missing test, incomplete implementation, or related improvement is small enough to be resolved inside the current PR, resolve it there. Creating a new issue is only justified when the work requires a separate product decision, a different owner, a human gate, or a scope that is genuinely outside the current issue's acceptance criteria.",
      "rationale": "Issue creation is not free. Every new issue added to the backlog consumes triage time, clutters the pipeline, and is statistically unlikely to be resolved \u2014 most small 'follow-up' issues are never actioned. Agents that defer solvable work into new issues are shifting cost onto humans and future pipeline runs.",
      "acceptance_criteria": [
        "No new GitHub issue is created by an agent for a defect or gap that is visible in the current diff and resolvable within the current issue's acceptance criteria.",
        "No new GitHub issue is created for work that would be completed in fewer than 30 lines of code and has no dependency on a human gate or external decision.",
        "When an agent creates a new issue, the PR description explains specifically why the work cannot be done inline and what external dependency or decision blocks it.",
        "Agents do not split a single coherent feature into multiple issues to reduce the scope of their current run."
      ],
      "anti_patterns": [
        "Opening a 'follow-up' issue for a missing test when the test could be written in the current PR.",
        "Creating an issue titled 'Clean up X' for code touched in the current diff.",
        "Filing an issue for a one-line bug fix found while implementing the current feature.",
        "Splitting 'implement feature' and 'document feature' into separate issues when documentation is an acceptance criterion of the feature.",
        "Creating a placeholder issue with no acceptance criteria as a way to acknowledge tech debt without fixing it."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer",
        "issue-classifier",
        "prd-writer"
      ]
    },
    {
      "id": "STD-ARCH-008",
      "title": "Derive data structure from a single nominated authoritative source",
      "description": "Field definitions, column names, data types, and labels are derived at runtime or build time from a single authoritative source of truth, never hardcoded in code that must then be kept in sync with it. Each project nominates that source -- the database schema, a configuration or schema file, or an equivalent registry -- in a project ADR at project start, and all schema-dependent code derives from it.",
      "rationale": "Hardcoded field and type definitions silently diverge from the real source and break schema-driven behaviour; a single nominated source keeps generated UI, validation, and serialisation consistent.",
      "acceptance_criteria": [
        "Schema-derived code in the diff reads field, type, and label definitions from the project's nominated authoritative source, not hardcoded literals.",
        "No competing hardcoded copy of the schema is introduced alongside the nominated source."
      ],
      "anti_patterns": [
        "Hardcoding a field list, column names, or type mappings in a component or module instead of deriving them from the nominated authoritative source."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-013",
      "related": [
        "STD-ARCH-020"
      ],
      "title": "Satisfy a declared return/interface contract completely on every path",
      "description": "When a function or handler declares a return or interface contract, every return path -- including error and early-return paths -- produces the complete required shape, never a partial or bare value.",
      "rationale": "Consumers rely on the full contract; a return path that produces a partial shape yields a silently-incomplete result downstream.",
      "acceptance_criteria": [
        "Every return path of a function/handler with a declared contract in the diff produces the complete required shape (all required fields set).",
        "Error and early-return paths satisfy the same contract (e.g. a well-formed failure object), not a bare, undefined, or partial value."
      ],
      "anti_patterns": [
        "Returning a partial object from one branch of a function whose declared contract requires the full shape."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-014",
      "title": "Fail closed on errors",
      "description": "On an error -- a failed query, a failed external call, or any operation that did not succeed -- fail closed: surface or throw the error. Do not silently swallow it and return a degraded, partial, or success-shaped result as if it succeeded.",
      "rationale": "Silently swallowing an error produces a degraded result that looks valid downstream, hiding integrity and availability failures.",
      "acceptance_criteria": [
        "Every fallible operation in the diff checks its error/result and stops or throws on failure rather than continuing.",
        "No catch block in the diff swallows an error and returns a success-shaped or default value without re-raising or recording the failure.",
        "A degraded result is emitted only where an explicit, documented fallback is required by the issue."
      ],
      "anti_patterns": [
        "Discarding an error return or exception and returning an empty or default value as though the operation succeeded."
      ],
      "adr_overridable": false,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-015",
      "title": "Verify framework APIs and conventions against existing usage before relying on them",
      "description": "Before using a framework or library API, parameter, or convention that is not already established in the codebase, confirm it against the framework's types or an existing usage rather than assuming it must work a certain way. If there is no precedent and no type to back it, verify before shipping.",
      "rationale": "Assuming an API or convention exists in a particular form, without checking, ships runtime-breaking code that type-checking and review miss.",
      "acceptance_criteria": [
        "A framework API, parameter, or convention used in the diff that has no precedent in the codebase is confirmed against the framework's types or an existing usage.",
        "The diff does not rely on a framework value or parameter inferred from first principles with no precedent or type to back it."
      ],
      "anti_patterns": [
        "Calling a library API with an assumed signature or option that is not backed by its types or any existing usage in the codebase."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-017",
      "related": [
        "STD-SEC-011"
      ],
      "title": "Access data through a middleware or API layer, never directly from the client",
      "description": "Client and front-end code must never connect to the database or data store directly. All data access goes through a middleware or API layer that centralises entitlements and authorization, security controls (validation, sanitisation, audit), and performance capabilities (caching, query shaping, limits). The specific middleware or API-layer design is specified in a project ADR early in the project, and all data access routes through it.",
      "rationale": "A single data-access layer keeps authorization, validation, auditing, and performance controls uniform; direct client-to-database access bypasses them and couples clients to storage structure.",
      "acceptance_criteria": [
        "Client/front-end code in the diff accesses data only through the project's nominated middleware/API layer, never a direct database connection or raw query.",
        "New data-access paths route through the layer so entitlements, validation, and auditing are applied uniformly."
      ],
      "anti_patterns": [
        "Client code issuing a raw database query or opening a direct database connection, bypassing the API/middleware layer."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-018",
      "title": "Change state through a dedicated validated transition, not a generic update",
      "description": "Lifecycle and status fields change only through a dedicated, validated state-transition path -- never an ad-hoc field update -- so transitions are checked against the allowed set, fire their side-effects, and are audited distinctly.",
      "rationale": "Mutating state via a generic update bypasses transition validation, side-effects, and the distinct audit trail, and allows invalid states.",
      "acceptance_criteria": [
        "Lifecycle/status fields in the diff change through a dedicated, validated transition path, not a generic update payload.",
        "An invalid or unlisted transition is rejected, not written."
      ],
      "anti_patterns": [
        "Setting a status or lifecycle field with a generic update call instead of an explicit, validated transition."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-020",
      "title": "Use a consistent API response envelope",
      "description": "API responses use a single, documented envelope with uniform shapes: success responses share a consistent structure and error responses a consistent error structure. Endpoints do not invent ad-hoc, per-endpoint response shapes.",
      "rationale": "A uniform envelope lets every consumer parse responses the same way; ad-hoc shapes break clients and leak internal structure.",
      "acceptance_criteria": [
        "Responses in the diff use the project's standard success and error envelope, not a bespoke per-endpoint shape.",
        "Error responses use the consistent error structure rather than an ad-hoc shape."
      ],
      "anti_patterns": [
        "Returning a bespoke JSON shape from one endpoint that differs from the project's standard success/error envelope."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-022",
      "related": [
        "STD-ARCH-026"
      ],
      "title": "Never expose raw internal database identifiers; expose stable external IDs",
      "description": "Raw database primary and foreign keys are never exposed through any external channel -- web UI, API responses, exports, logs, or tool input/output. Expose a stable external identifier instead. The external-identifier scheme is a project decision.",
      "rationale": "Raw internal keys leak storage structure and are unstable across environments; a stable external identifier decouples consumers from the database and avoids enumerable-ID exposure.",
      "acceptance_criteria": [
        "No diff surfaces a raw primary or foreign key through any channel -- UI, API response, export, log, or tool I/O.",
        "Identifier values crossing an external boundary in the diff use the project's stable external-identifier scheme."
      ],
      "anti_patterns": [
        "Returning a raw numeric or UUID primary key in an API response, URL, export, or log line."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-023",
      "title": "Do not use deprecated language or DSL constructs; use the current supported form",
      "description": "Code and DSL/expression authoring use the current, supported syntax and APIs -- not deprecated ones that emit warnings or are scheduled for removal.",
      "rationale": "Deprecated constructs warn now and break later; using the current supported form keeps code forward-compatible.",
      "acceptance_criteria": [
        "The diff does not introduce a deprecated language, DSL, or API construct where a current supported form exists.",
        "Deprecated constructs in code or expressions touched by the diff are migrated to the current form."
      ],
      "anti_patterns": [
        "Using a deprecated API or language/DSL construct when a current supported form exists."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-025",
      "title": "Avoid N+1 queries and unbounded reads; respect platform query limits",
      "description": "Avoid N+1 query patterns -- bulk-fetch related data by deduplicating IDs into a single query rather than one query per record -- and bound large reads with pagination, respecting the data platform's row limits.",
      "rationale": "Per-record fetches produce N+1 query storms, and a silent default row cap truncates large reads.",
      "acceptance_criteria": [
        "Related-data fetches in the diff collect and deduplicate IDs and issue a single bulk query, not one per record.",
        "Reads against potentially-large tables use pagination rather than relying on the platform's default row cap.",
        "No diff issues a query inside a per-record loop where a single bulk query would suffice."
      ],
      "anti_patterns": [
        "Issuing one query per record in a loop instead of a single bulk query; reading an unbounded result set without pagination."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-026",
      "related": [
        "STD-ARCH-022"
      ],
      "title": "Do not use `as any`/non-null assertions to silence the type system at the data boundary",
      "description": "Type assertions that defeat the compiler -- `as any`, double casts, and non-null assertions -- must not be used to force values across a data boundary. Where a value's runtime shape differs from its static type, validate or convert it explicitly rather than asserting.",
      "rationale": "Assertions that defeat the compiler hide real shape mismatches at data boundaries; correcting the type or validating the value surfaces them.",
      "acceptance_criteria": [
        "No diff introduces `as any` or a double cast to push a value across a data boundary.",
        "No diff adds a non-null assertion to suppress a nullability or shape error instead of handling it.",
        "Where a declared type mismatches the runtime shape, the diff fixes the type or parses/validates the value rather than casting."
      ],
      "anti_patterns": [
        "Using `as any` or a non-null assertion to force a value across the data boundary instead of validating it."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "source": "Generalised to org from a project-migrated standard"
    },
    {
      "id": "STD-ARCH-029",
      "title": "Deployment environments use the fixed names PROD, BETA, TEST, DEV, TEMP",
      "description": "Every deployment environment is named from a single fixed vocabulary of five upper-case tiers: PROD (production -- the live, user-facing environment; the only tier that serves real users and holds real production data), BETA (pre-production/staging -- a production-like environment for release-candidate validation and acceptance before promotion to PROD), TEST (integration and QA -- where automated and manual test suites run against integrated services), DEV (development -- the shared, expected-unstable environment for in-progress work), and TEMP (ephemeral -- short-lived throwaway environments such as per-branch previews or experiments, provisioned on demand and torn down automatically). Environment identifiers in configuration, infrastructure code, hostnames, variable names, and secret scopes use one of these five names. Ad-hoc synonyms (staging, uat, qa, preprod, prod2, sandbox, local) are not introduced, and a tier beyond the five requires a deliberate decision recorded in an ADR.",
      "rationale": "A single fixed environment vocabulary makes every deploy target, config key, hostname, and secret scope unambiguous across services and tooling; ad-hoc synonyms (staging vs uat vs preprod) fragment configuration and cause the wrong environment to be targeted.",
      "acceptance_criteria": [
        "New or renamed deployment environments in the diff use exactly one of PROD, BETA, TEST, DEV, TEMP (upper-case).",
        "Environment identifiers in configuration, infrastructure code, hostnames, and secret scopes in the diff draw from the same five-name vocabulary, not ad-hoc synonyms (staging, uat, qa, prod2, sandbox).",
        "Only PROD serves real users and holds real production data; lower tiers do not point at production resources.",
        "A tier outside the five, or a synonym for an existing tier, is introduced only under a referenced ADR."
      ],
      "anti_patterns": [
        "Naming an environment `staging` or `uat` instead of `BETA`.",
        "Introducing `prod2`, `qa1`, or `sandbox` as a parallel environment name.",
        "Using lower-case or mixed-case environment names (`Prod`, `dev-env`) inconsistently across services."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-030",
      "title": "Cloud resources follow a consistent, type-encoded naming convention",
      "description": "Every cloud resource is named `{app}-{sub-app}-{code}[-{env}][-{region}][-{instance}]`, where `{sub-app}`, `{env}`, `{region}`, and `{instance}` are optional and `{code}` is the resource type's official provider abbreviation, upper-cased (Azure: the Cloud Adoption Framework resource abbreviations; AWS and Google Cloud: their equivalent resource-type conventions). `{env}` is the STD-ARCH-029 environment identifier and its ABSENCE means production (PROD); `{region}` is an approved region code (STD-ARCH-032), omitted for global or single-region resources; `{instance}` is a zero-padded number (e.g. 001) for scaled-out or duplicate resources. On Azure the resource group (the grouping construct) is named `{app}-RG[-{region}][-{env}]`. Segments are hyphen-delimited and a given resource type always uses the same `{code}`. Where a provider's rules for a resource type forbid hyphens or upper-case or cap length (e.g. Azure storage accounts, AWS S3 / Google Cloud Storage buckets: lower-case, no hyphens, globally unique, length-limited), the name uses that type's valid compacted/lower-cased form of the same components. Reference the provider's official abbreviation list rather than reproducing it.",
      "rationale": "A predictable, type-encoded naming scheme makes ownership, environment, region, and resource type readable at a glance across every provider's console, infrastructure-as-code, cost reports, and alerts, and lets automation target resources by pattern.",
      "acceptance_criteria": [
        "Any cloud resource defined or renamed in the diff is named `{app}-{sub-app}-{code}[-{env}][-{region}][-{instance}]` (e.g. `payments-web-KV`, `payments-ACS-DEV`, `payments-web-VM-DEV-EUS2-001`); an Azure resource group is `{app}-RG[-{region}][-{env}]` (e.g. `payments-RG`, `payments-RG-DEV`).",
        "The `{code}` segment is the resource type's official provider abbreviation, upper-cased -- for Azure take it from the CAF resource-abbreviations reference (https://learn.microsoft.com/en-us/azure/cloud-adoption-framework/ready/azure-best-practices/resource-abbreviations); do not invent ad-hoc codes -- and a resource of a given type always uses the same `{code}`.",
        "`{env}` uses the STD-ARCH-029 vocabulary and its absence means PROD; `{region}` uses STD-ARCH-032; `{instance}` is a zero-padded number. Omitted optional segments stay unambiguous (env vocabulary, region codes, and numeric instance do not overlap).",
        "Where the provider forbids hyphens or upper-case or caps length for a resource type (Azure storage account, S3 / Cloud Storage bucket, Key Vault, etc.), the name is a valid compacted form of the same components, not an invalid one."
      ],
      "anti_patterns": [
        "A resource group named without the `RG` code, or for a person or another type (e.g. `payments` without `-RG`, `johns-rg`).",
        "A resource name that omits the resource-type `{code}`, or invents an ad-hoc abbreviation instead of the provider's official one.",
        "Inconsistent type codes for the same resource type (e.g. `KV` in one place and `keyvault` in another).",
        "A name that is invalid for its resource type (e.g. `payments-web-ST` for a storage account or bucket, which cannot contain hyphens or upper-case)."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-031",
      "title": "Cloud resources carry a mandatory core tag or label set",
      "description": "Every cloud resource carries a core set of governance metadata -- tags on Azure and AWS, labels on Google Cloud: `Environment` (the STD-ARCH-029 identifier, or PROD when the name has no environment segment), `ApplicationName` (the owning application or service), `Owner` (accountable team or email), `CostCenter` (chargeback code), `Criticality` (business criticality), and `DataClassification` (e.g. Public/Internal/Confidential/Restricted). Keys and values are spelled and cased consistently per the provider's rules (note Google Cloud labels require lower-case keys and values and a restricted character set). Tags and labels never contain secrets, credentials, or personal data, since they surface in reports and logs. Follow each provider's tagging or labelling guidance (Azure CAF tagging, AWS tagging best practices, Google Cloud labels) rather than reproducing it here.",
      "rationale": "Consistent metadata makes cost allocation, ownership, incident routing, compliance, and lifecycle automation possible across every provider and subscription; a mandatory core set prevents untagged, unattributable resources.",
      "acceptance_criteria": [
        "Any cloud resource defined in the diff sets at least the core metadata -- Environment, ApplicationName, Owner, CostCenter, Criticality, DataClassification -- as tags on Azure/AWS or labels on Google Cloud.",
        "Keys and values are consistent and conform to the provider's rules (e.g. Google Cloud labels are lower-case); DataClassification is one of the documented values (Public/Internal/Confidential/Restricted).",
        "No tag or label key or value contains a secret, credential, password, or personal data.",
        "The Environment metadata is consistent with the name: a resource with no naming `{env}` segment is tagged/labelled Environment=PROD."
      ],
      "anti_patterns": [
        "A resource created with no tags/labels, or missing one of the core keys.",
        "Storing a connection string, key, password, or personal data in a tag or label value.",
        "Inconsistent keys for the same concept (e.g. `env` vs `Environment` vs `environment`)."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-032",
      "title": "Region codes come from an approved region vocabulary",
      "description": "The `{region}` name segment (STD-ARCH-030) and any region tag or label use a short, upper-case region code from the org's approved, documented region vocabulary -- one code per provider region, applied consistently. The vocabulary maps each code to the provider's official region identifier; reference the provider's region list (Azure, AWS, Google Cloud) rather than reproducing it. A region not yet in the vocabulary is added to the documented set (mapped to the official region identifier) before use, not abbreviated ad hoc in a name or tag.",
      "rationale": "A single region-code vocabulary per org keeps names, tags, and tooling aligned across providers and prevents the same region appearing under several ad-hoc abbreviations.",
      "acceptance_criteria": [
        "A `{region}` segment or region tag/label in the diff is an upper-case code from the approved region vocabulary, one code per provider region, mapped to the provider's official region identifier.",
        "A given region always uses the same code across names and tags/labels.",
        "A region not yet in the vocabulary is added to the documented set (mapped to its official region identifier) in the same change, not used ad hoc."
      ],
      "anti_patterns": [
        "Two different codes for the same region (e.g. `EUS2` and `EASTUS2`).",
        "A raw provider region identifier in one place and a short code elsewhere for the same region."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-033",
      "title": "Naming and tagging are enforced by the cloud's native policy engine",
      "description": "Naming (STD-ARCH-030) and mandatory tagging or labelling (STD-ARCH-031) are enforced by the cloud's native policy engine -- Azure Policy, AWS (Service Control Policies, Config rules, or tag policies), or Google Cloud Organization Policy -- not left to convention alone. Policies audit or deny resources that violate the naming pattern or lack the core tags/labels (deny for new resources where practical, audit otherwise), so drift is caught at deploy time rather than only in review. Policy definitions and assignments are version-controlled as infrastructure-as-code alongside the resources they govern.",
      "rationale": "Convention without enforcement drifts; the cloud's native policy engine makes naming and tagging self-enforcing across every account, subscription, and team without manual review.",
      "acceptance_criteria": [
        "Infrastructure-as-code that provisions cloud resources includes or references native policy assignments (Azure Policy / AWS SCP or Config / Google Cloud Org Policy) enforcing the naming pattern (STD-ARCH-030) and the core tags/labels (STD-ARCH-031).",
        "New-resource violations are denied where practical, otherwise audited and surfaced to the owning team.",
        "Policy definitions and assignments are version-controlled as code, not applied only in the console or portal."
      ],
      "anti_patterns": [
        "Relying solely on code review to catch naming or tagging violations, with no native policy enforcement.",
        "Applying policy assignments manually in the console/portal so they are not reproducible or version-controlled."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-034",
      "title": "DNS Records Must Be Defined as Code",
      "description": "Every DNS record for a product-offering domain or subdomain (A/AAAA/CNAME/MX/TXT/NS/SRV) is declared in a version-controlled DNS-as-code definition (e.g. Terraform, OctoDNS, Pulumi) and applied only through the automated pipeline; no record is created or edited directly in a DNS provider console or via ad hoc API/CLI calls.",
      "rationale": "Manually created records are the largest source of DNS drift and orphaned entries; per STD-ARCH-033's precedent of enforcing infra invariants through a policy engine rather than convention, DNS ownership must be auditable from a diff, not tribal knowledge in a registrar console.",
      "acceptance_criteria": [
        "Every new or changed DNS record in the diff appears as a resource/record definition inside the repository's DNS-as-code configuration (e.g. a Terraform `*_record` resource, an OctoDNS zone file entry).",
        "The diff contains no manual-change evidence such as a screenshot, ticket reference, or comment instructing a human to edit records in a provider console.",
        "Each record definition includes an owning team or service label as a resource tag/comment."
      ],
      "anti_patterns": [
        "A PR description says 'ask the SRE on-call to add a CNAME for foo.product.com in Cloudflare' instead of adding the record to the zone file.",
        "A record is added via `aws route53 change-resource-record-sets` in a runbook step rather than through the Terraform/OctoDNS module."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ],
      "related": [
        "STD-ARCH-033"
      ]
    },
    {
      "id": "STD-ARCH-035",
      "title": "Orchestrator coordinates only; process steps live in pipeline.json as scripts or agents",
      "description": "pipeline/pipeline_orchestrator.py decides which step runs next and records the outcome. It must not contain business-process logic. Every step that enables the process is declared in pipeline/pipeline.json and implemented as a deterministic script under .github/scripts/ or as an agent prompt under .claude/agents/. Filesystem management, git operations, GitHub API workflows, and any other work performed around an agent belong in a script the orchestrator invokes, not in the orchestrator itself.",
      "rationale": "P-14 makes the orchestrator the deterministic router: its job is sequencing, mutex, and label transitions. Process logic embedded in it is untestable in isolation, invisible to pipeline.json (so the pipeline no longer describes itself), and inflates a module that already carries most of the package. Externalised steps are independently runnable, independently testable, and swappable without touching the router.",
      "acceptance_criteria": [
        "A new process step is added by declaring it in pipeline.json and adding a script under .github/scripts/ or an agent under .claude/agents/, with no new branching in pipeline_orchestrator.py beyond invoking the declared step.",
        "Work performed around an agent invocation (setup, teardown, commit, publish) is implemented in a script that can be run standalone with its documented environment variables, not inline in the orchestrator.",
        "The orchestrator holds no module-level mutable state describing an in-flight step's side effects; a step's own script is responsible for making its effects idempotent.",
        "Every environment variable a script needs is passed through a named per-site allowlist (STD-SEC-022), never by inheriting the orchestrator's environment."
      ],
      "anti_patterns": [
        "Adding filesystem create/remove calls to the orchestrator instead of a setup/teardown script it invokes.",
        "Encoding a step's ordering or preconditions in Python conditionals rather than in pipeline.json's trigger and dependencies fields.",
        "A module-level global tracking an in-flight step so a signal handler can undo its side effects, where an idempotent script would self-heal on the next run.",
        "Growing pipeline_orchestrator.py with logic that has no counterpart in pipeline.json, so reading the config no longer tells you what the pipeline does."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    },
    {
      "id": "STD-ARCH-036",
      "title": "Destructive or state-changing infrastructure jobs support a dry-run mode",
      "description": "Any script, job, or workflow that can create, modify, or destroy infrastructure, stored data, or a running service -- a deploy, a migration, a bulk data operation, a resource-deletion sweep -- accepts a dry-run mode, designed in from the start rather than added after an incident. Dry-run performs every read and computation the real run would, but skips every write, apply, or delete call, and reports exactly what would change.",
      "rationale": "A destructive job's blast radius is only visible once it runs. A dry-run mode that reports the same plan the real run would produce, without applying it, is the only reliable way to verify intent before a change becomes irreversible; \"test it in staging first\" is not a substitute, since staging drifts from production and does not cover every environment the job can target.",
      "acceptance_criteria": [
        "Every new script, job, or workflow in the diff that can create, modify, or delete infrastructure, stored data, or a running service exposes an explicit dry-run mode (a flag, input, or equivalent) that performs the real run's reads and computations but skips every write/apply/delete call.",
        "The dry-run mode's output names exactly what would change -- which resources, records, or rows -- not merely that changes exist.",
        "A destructive job shipped with no dry-run mode carries a stated, ADR-recorded justification for why one is not possible."
      ],
      "anti_patterns": [
        "A deploy or migration script that only supports running for real, with 'test it in staging first' as the sole safety net.",
        "A --dry-run flag that exists but silently performs a subset of the real writes anyway.",
        "A bulk-delete, cleanup, or reclamation job added with no way to preview what it would remove before it removes it."
      ],
      "adr_overridable": true,
      "applies_to": [
        "coder",
        "pr-reviewer"
      ]
    }
  ]
}
