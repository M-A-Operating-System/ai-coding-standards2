<!-- ai-agile/snapshot/v1 by 01_product_docs/prd-writer -->
## Original issue (snapshot before PRD rewrite)

**Original title:** [#142 - 3/6] Extract per-agent hooks to post_steps scripts

**Original body:**

## Context

Part 3 of 6 — implements the third phase of the orchestrator simplification described in #142.

## Problem Statement

After Part 1 introduces `commit-agent-work.sh`, any remaining agent-specific post-run hooks in the orchestrator (e.g. `mark_ready_on_complete`, PR-ready transitions, label cleanup) should be extracted to per-agent `post_steps` shell scripts and declared in `pipeline.json`. The orchestrator should loop over `post_steps` and execute them — no agent-specific logic should remain in Python.

## Acceptance Criteria

- [ ] `mark_ready_on_complete` logic extracted to `.github/scripts/mark-pr-ready.sh`; wired as a `post_steps` entry for `03_execute/pr-reviewer` in `pipeline.json`.
- [ ] Any other agent-specific post-run hooks in the orchestrator extracted similarly.
- [ ] Orchestrator `post_steps` loop executes scripts in declared order; failure exits with non-zero so the orchestrator applies `:failed`.
- [ ] No agent-specific `if agent == "..."` branches remain in the orchestrator's post-run path.
- [ ] Existing CI passes.

## Dependencies

Requires Part 1 (#151) — the `post_steps` pattern is established there.
